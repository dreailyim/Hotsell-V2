
{
  "functions": {
    "source": "functions",
    "predeploy": [
      "npm --prefix functions install"
    ]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules",
    "cors": "cors.json"
  }
}
