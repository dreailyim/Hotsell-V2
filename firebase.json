{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": {
    "source": ".",
    "runtime": "nodejs20",
    "predeploy": [
      "npm install",
      "npm run build:functions"
    ]
  },
  "storage": {
    "rules": "storage.rules"
  }
}
