// This file is intentionally left empty.
// It serves as a placeholder to ensure the /chat/[chatId] route group
// correctly inherits the layout from the (main) layout group.
export default function ChatRedirectPage() {
  return null;
}
